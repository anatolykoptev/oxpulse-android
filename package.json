{
  "name": "oxpulse-android",
  "version": "0.0.0",
  "private": true,
  "description": "Android shell for OxPulse — native call-reliability around the OxPulse web app",
  "license": "AGPL-3.0-only",
  "scripts": {
    "sync": "cap sync android",
    "build:debug": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:release": "cap sync android && cd android && ./gradlew assembleRelease bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1",
    "typescript": "^5.6.0"
  }
}
